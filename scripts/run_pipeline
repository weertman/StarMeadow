#!/usr/bin/env bash
set -euo pipefail

# Backward-compatible wrapper for the publication-only build pipeline.
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$REPO_ROOT/scripts/build_publication_outputs" "$@"
